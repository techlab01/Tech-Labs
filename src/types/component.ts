export type layoutPropsType = {
  children: React.ReactNode;
  showHeader?: boolean;
  showContactInfo?: boolean;
  showFooter?: boolean;
};

export type bannerType = {
  bannerText: string;
  bannerImg: string;
};
